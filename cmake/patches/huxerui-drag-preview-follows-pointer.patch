diff --git a/src/runtime/runtime_pointer_interaction.cpp b/src/runtime/runtime_pointer_interaction.cpp
index dad3775..7a2f606 100644
--- a/src/runtime/runtime_pointer_interaction.cpp
+++ b/src/runtime/runtime_pointer_interaction.cpp
@@ -297 +297 @@ LayerPlacement DragPreviewPlacement(Point position, Point grab_offset) {
-      .kind = LayerPlacementKind::Anchored,
+      .kind = LayerPlacementKind::FollowPointer,
@@ -299,4 +298,0 @@ LayerPlacement DragPreviewPlacement(Point position, Point grab_offset) {
-      .preferred_side = LayerAnchorSide::Below,
-      .alignment = LayerAnchorAlignment::Start,
-      .gap = 0.0F,
-      .viewport_margin = 8.0F,
diff --git a/src/runtime/ui_window.cpp b/src/runtime/ui_window.cpp
index 9f44442..a6d1d1b 100644
--- a/src/runtime/ui_window.cpp
+++ b/src/runtime/ui_window.cpp
@@ -500,0 +501,12 @@ public:
+    case LayerPlacementKind::FollowPointer: {
+      // Drag feedback belongs to the pointer, not to the viewport. Popover
+      // anchor rules (side flipping and viewport clamping) make a dragged
+      // item jump away from the grab point near the window edges.
+      child_size = context.Measure(child, loose);
+      const EdgeInsets layer_padding = static_cast<detail::MountedNode&>(node).resolved_padding;
+      child_offset = {
+          placement.anchor.x - layer_padding.left + placement.offset.x,
+          placement.anchor.y - layer_padding.top + placement.offset.y,
+      };
+      break;
+    }
diff --git a/src/runtime/ui_window_internal.h b/src/runtime/ui_window_internal.h
index 2172fa6..cc5a977 100644
--- a/src/runtime/ui_window_internal.h
+++ b/src/runtime/ui_window_internal.h
@@ -140,0 +141 @@ enum class LayerPlacementKind : std::uint8_t {
+  FollowPointer,
